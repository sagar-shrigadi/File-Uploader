<% if (locals.errors) { %>
    <article class="errors">
        <ul>
            <% errors.forEach(err => { %>
                <li>
                    <%= err.msg %>
                </li>
            <% }) %>
        </ul>
    </article>
<% } %>